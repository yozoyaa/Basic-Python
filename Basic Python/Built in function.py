x = 3.14
y = 4
z = 5

# result = round(x)     # Rounded the number
# result = abs(y)       # absolute value of a number change ganti y jadi -4 sebelum gunakain ini
result = pow(4, 3)      # << Dibaca 4 pangkat 3 Dalam konteks aritmetika, "pow" biasanya merujuk pada fungsi atau operasi pangkat. Istilah "pow"
                        # seringkali digunakan dalam pemrograman dan matematika untuk menandakan operasi yang menghitung
                        # suatu bilangan (basis) dipangkatkan dengan bilangan lain (eksponen)
# result = max(x, y, z) # mencari maximum value dari 3 variable
# result = min(x, y ,z) # mencari minimum value dari 3 variable

print(result)